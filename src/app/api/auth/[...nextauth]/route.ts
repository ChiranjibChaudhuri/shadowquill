// Re-export the handlers from the main auth initialization file
export { GET, POST } from '@/auth';

// You might need this line if your deployment environment requires explicit runtime definition
// export const runtime = "edge"; // or "nodejs"
